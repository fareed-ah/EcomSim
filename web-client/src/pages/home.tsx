import React from 'react'
import { NavBar } from '../components/navbar/NavBar';

interface homeProps {
}

const Home: React.FC<homeProps> = ({ }) => {
    return (
        <NavBar />
    );
}

export default Home;